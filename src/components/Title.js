import React from "react";
import withStyles from "./withStyles";

// const Title = ({ children }) => {
//   return <div>{children}</div>;
// };
const Title = () => <p>test</p>;

export default withStyles(Title);
